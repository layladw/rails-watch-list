<h1>Lists</h1>

  <% @lists.each do |list| %>
  <div class="list">
    <h2> <%= link_to list.name, list_path(list) %> </h2>
  </div>
  <% end %>

<button class="btn-grey"> <%= link_to 'Add a new list', new_list_path %></button>
